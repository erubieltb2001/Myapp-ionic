import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mexicanfood.app',
  appName: 'mexican-food',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
